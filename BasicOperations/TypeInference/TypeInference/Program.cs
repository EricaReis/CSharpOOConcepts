using System;

namespace TypeInference {
    class Program {
        static void Main(string[] args)
        {
            // The compiler automatically deduces the type of the variable through type inference.
            var x = 10;
            var y = 20.0;
        }
    } 
}
